version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=azuredevopsdevsecopsorgmine_java-vulneravel -Dsonar.organization=azuredevsecopsorgmine -Dsonar.host.url=https://sonarcloud.io/ -Dsonar.login=3ca521c7115e3bbac50ed922d4bdd317c204c059
